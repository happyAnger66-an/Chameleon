"""pi05 deploy / TRT build 共享形状常量 — export 与 compile 须保持一致。"""

from __future__ import annotations

# pi05 LIBERO（3 路相机 × 256 image tokens + language tokens）典型 prefix 长度。
PI05_LIBERO_PREFIX_LEN = 968
